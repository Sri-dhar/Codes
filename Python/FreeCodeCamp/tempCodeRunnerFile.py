for name in names:
    #     if len(name) > longest:
    #         longest = len(name)
    # for i in range(longest):
    #     line = "     "
    #     for j in range(len(names)):
    #         if i < len(names[j]):
    #             line += names[j][i] + "  "
    #         else:
    #             line += "   "
    #     heading += line
    #     if i < longest - 1:
    #         heading += "\n"